spring:
  application:
    name: Scrap Location

  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://patria-mysql:3306/ms_scrap_locations?allowMultiQueries=true&characterEncoding=UTF-8&useUnicode=true
    username: "user_scrap_locations"
    password: "ms.SLocations.1"

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect

  flyway:
    enabled: true
    baseline-on-migrate: true
    locations: classpath:db/migration